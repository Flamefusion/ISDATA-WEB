services:
  - name: ch-api
    type: web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ch-db
          property: connectionString
      - key: FLASK_ENV
        value: production

databases:
  - name: ch-db
    plan: free
